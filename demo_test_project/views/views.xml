<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="wp_module.list">
      <field name="name">wp_module list</field>
      <field name="model">wp_module.wp_module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="wp_module.action_window">
      <field name="name">wp_module window</field>
      <field name="res_model">wp_module.wp_module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="wp_module.action_server">
      <field name="name">wp_module server</field>
      <field name="model_id" ref="model_wp_module_wp_module"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="wp_module" id="wp_module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="wp_module.menu_1" parent="wp_module.menu_root"/>
    <menuitem name="Menu 2" id="wp_module.menu_2" parent="wp_module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="wp_module.menu_1_list" parent="wp_module.menu_1"
              action="wp_module.action_window"/>
    <menuitem name="Server to list" id="wp_module" parent="wp_module.menu_2"
              action="wp_module.action_server"/>
    -->
  </data>
</odoo>